COMPOSE_PROJECT_NAME='nginx-proxy-manager'
#VOLUMES='./.volumes'

# Note: Names of docker assets (such as volumes) must be globally unique
# (using project name as prefix avoids conflicts).
VOLUME_DATA="${COMPOSE_PROJECT_NAME}_data"
VOLUME_LETSENCRYPT="${COMPOSE_PROJECT_NAME}_letsencrypt"
